<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/bubble"
    tools:context=".ShowPrayAlarmActivity"
    android:gravity="center">

    <TextView
        android:id="@+id/prayer_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_horizontal"
        android:textColor="@color/white"
        android:textSize="24sp"/>


    <com.sufi.prayertimes.customWidget.swipeButton.SwipeButton
        android:id="@+id/swipe_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="30dp"
        android:layout_marginTop="30dp"
        android:layout_marginEnd="30dp"
        android:layout_marginBottom="26dp"
        app:button_background="@drawable/swipe_button_shape"
        app:inner_text_background="@drawable/swipe_shape_rounded"
        app:button_image_disabled="@drawable/ic_alarm_on"
        app:button_image_enabled="@drawable/ic_alarm_off"
        app:button_bottom_padding="18dp"
        app:button_left_padding="18dp"
        app:button_right_padding="18dp"
        app:button_top_padding="18dp"
        app:inner_text="SWIPE TO OFF"
        app:inner_text_top_padding="@dimen/space_twenty"
        app:inner_text_bottom_padding="@dimen/space_twenty"
        app:inner_text_color="@color/white"
        app:inner_text_size="@dimen/spacing_normal"
        />


</LinearLayout>